generator client {
  provider      = "prisma-client-js"
  // Include linux-musl binary so the Docker runner stage (node:22-alpine)
  // can load Prisma. Without this, the engine binary built on the Debian
  // builder stage cannot run on Alpine.
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Profile {
  id                 String       @id @default(cuid())
  userId             String       @unique
  firstName          String?
  lastName           String?
  email              String?
  phone              String?
  location           String?
  professionalLinks  Json?
  headline           String?
  summary            String?
  targetRoles        String[]
  targetLocations    String[]
  workModePreference String?
  salaryPreference   Int?
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt
  educations         Education[]
  experiences        Experience[]
  skills             Skill[]
}

model Experience {
  id           String         @id @default(cuid())
  profileId    String
  type         ExperienceType
  title        String
  organization String?
  startDate    DateTime?
  endDate      DateTime?
  isCurrent    Boolean        @default(false)
  description  String?
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  location     String?
  order        Int            @default(0)
  profile      Profile        @relation(fields: [profileId], references: [id], onDelete: Cascade)
}

model Education {
  id           String    @id @default(cuid())
  profileId    String
  institution  String
  degree       String?
  fieldOfStudy String?
  startDate    DateTime?
  endDate      DateTime?
  gpa          String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  profile      Profile   @relation(fields: [profileId], references: [id], onDelete: Cascade)
}

model Skill {
  id          String   @id @default(cuid())
  profileId   String
  name        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  category    String?
  order       Int      @default(0)
  proficiency String?
  profile     Profile  @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@unique([profileId, name])
}

model Job {
  id                String            @id @default(cuid())
  userId            String
  title             String
  company           String
  location          String?
  description       String?
  compensationNotes String?
  applicationDate   DateTime?
  stage             JobStage          @default(INTERESTED)
  priority          Boolean           @default(false)
  deadline          DateTime?
  recruiterNotes    String?
  customNotes       String?
  lastActivityAt    DateTime?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  companyResearch   String?
  prepNotesStar             String?
  prepNotesQuestions        String?
  prepNotesTalkingPoints    String?
  activities        JobActivity[]
  documentLinks     JobDocumentLink[]
  stageHistory      JobStageHistory[]

  @@index([userId])
  @@index([userId, stage])
  @@index([userId, lastActivityAt])
}

model JobStageHistory {
  id        String    @id @default(cuid())
  jobId     String
  fromStage JobStage?
  toStage   JobStage
  changedAt DateTime  @default(now())
  job       Job       @relation(fields: [jobId], references: [id], onDelete: Cascade)

  @@index([jobId, changedAt])
}

model JobActivity {
  id          String    @id @default(cuid())
  jobId       String
  type        String
  title       String
  description String?
  scheduledAt DateTime?
  roundType   String?
  completed   Boolean   @default(false)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  job         Job       @relation(fields: [jobId], references: [id], onDelete: Cascade)

  @@index([jobId, createdAt])
}

model Document {
  id             String            @id @default(cuid())
  userId         String
  type           DocumentType
  name           String
  category       String?
  status         DocumentStatus    @default(DRAFT)
  previousStatus DocumentStatus?
  isDeleted      Boolean           @default(false)
  deletedAt      DateTime?
  createdAt      DateTime          @default(now())
  updatedAt      DateTime          @updatedAt
  versions       DocumentVersion[]
  jobLinks       JobDocumentLink[]

  @@index([userId, isDeleted])
  @@index([userId, updatedAt])
}

model DocumentVersion {
  id            String            @id @default(cuid())
  documentId    String
  versionNumber Int
  content       String?
  fileUrl       String?
  createdAt     DateTime          @default(now())
  document      Document          @relation(fields: [documentId], references: [id], onDelete: Cascade)
  jobLinks      JobDocumentLink[]

  @@unique([documentId, versionNumber])
  @@index([documentId, versionNumber])
}

model JobDocumentLink {
  id                String          @id @default(cuid())
  jobId             String
  documentId        String
  documentVersionId String
  linkedAt          DateTime        @default(now())
  document          Document        @relation(fields: [documentId], references: [id], onDelete: Cascade)
  documentVersion   DocumentVersion @relation(fields: [documentVersionId], references: [id], onDelete: Cascade)
  job               Job             @relation(fields: [jobId], references: [id], onDelete: Cascade)

  @@unique([jobId, documentVersionId])
  @@index([jobId, linkedAt])
  @@index([documentId])
}

model UserSettings {
  id          String   @id @default(cuid())
  userId      String   @unique
  preferences Json     @default("{}")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

enum JobStage {
  INTERESTED
  APPLIED
  INTERVIEW
  OFFER
  REJECTED
  ARCHIVED
  GHOSTED
}

enum ExperienceType {
  EMPLOYMENT
  PROJECT
}

enum DocumentType {
  RESUME
  COVER_LETTER
  OTHER
}

enum DocumentStatus {
  DRAFT
  READY
  ARCHIVED
  UPLOADED
}
